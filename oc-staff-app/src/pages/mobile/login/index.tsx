import React from 'react';
import MobileLogin from '../../auth/MobileLogin'; // MobileLogin コンポーネントをインポート

const LoginPage: React.FC = () => {
    return(
        <MobileLogin />
    )
};

export default LoginPage;